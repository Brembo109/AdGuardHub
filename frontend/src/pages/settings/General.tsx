/** Timers and limits — what the hub does on its own schedule. */

import { HubSettingsForm } from '../../components/HubSettingsForm'

export default function SettingsGeneral() {
  return <HubSettingsForm />
}
