/** Whether a newer AdGuardHub exists, and what this install can do about it. */

import { UpdateCard } from '../../components/UpdateCard'

export default function SettingsUpdates() {
  return <UpdateCard />
}
